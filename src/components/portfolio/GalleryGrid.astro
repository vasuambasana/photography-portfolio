---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div id="portfolio-grid" class:list={[
  'grid grid-cols-1 md:grid-cols-2 gap-2 md:gap-4 stagger-children',
  className,
]}>
  <slot />
</div>
