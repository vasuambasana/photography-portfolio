---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div id="portfolio-grid" class:list={[
  'grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8 stagger-children transition-all duration-500',
  className,
]}>
  <slot />
</div>
