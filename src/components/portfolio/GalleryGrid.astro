---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div id="portfolio-grid" class:list={[
  'columns-1 sm:columns-2 lg:columns-3 xl:columns-4 gap-3 md:gap-4 stagger-children [column-fill:_balance]',
  className,
]}>
  <slot />
</div>

