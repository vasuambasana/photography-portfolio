---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={[
  'grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8 stagger-children',
  className,
]}>
  <slot />
</div>
